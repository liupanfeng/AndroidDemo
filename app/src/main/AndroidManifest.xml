<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.liupf.androidstudy">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.GET_TASKS" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".KeyBoardActivity"
            android:theme="@style/NotitleTheme" />
        <activity
            android:name=".FloatWindowActivity"
            android:theme="@style/NotitleTheme" />

        <service
            android:name=".service.FloatWindowService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".AttributeAnimationActivity" />
        <activity android:name=".MaterialDesignPaletteActivity"></activity>
    </application>

</manifest>